diff --git a/dist/modules/asyncRequire.ts b/dist/modules/asyncRequire.ts
index 5445e34463427d0d9396da0a940b1d8059576ece..297300ef3d62296b9d8ebc0bade6d7afd3c6a4e3 100644
--- a/dist/modules/asyncRequire.ts
+++ b/dist/modules/asyncRequire.ts
@@ -65,7 +65,6 @@ function buildLoadBundleAsyncWrapper() {
   const registry = require('mf:remote-module-registry');
 
   const __loadBundleAsync =
-    // @ts-expect-error dynamic key access on global object
     globalThis[`${__METRO_GLOBAL_PREFIX__ ?? ''}__loadBundleAsync`];
 
   const loadBundleAsync =
@@ -120,15 +119,13 @@ if (!process.env.EXPO_OS) {
   // process.env.EXPO_OS to be set but since expo is optional, we set it
   // to an empty string as a fallback to prevent reference errors
   process.env.EXPO_OS = '';
+}
 
-  const {
-    buildAsyncRequire,
-  } = require('@expo/metro-runtime/src/async-require/buildAsyncRequire');
+const {
+  buildAsyncRequire,
+} = require('@expo/metro-runtime/src/async-require/buildAsyncRequire');
 
-  // @ts-expect-error dynamic key access on global object
-  global[`${__METRO_GLOBAL_PREFIX__}__loadBundleAsync`] = buildAsyncRequire();
-}
+global[`${__METRO_GLOBAL_PREFIX__}__loadBundleAsync`] = buildAsyncRequire();
 
-// @ts-expect-error dynamic key access on global object
 global[`${__METRO_GLOBAL_PREFIX__}__loadBundleAsync`] =
   buildLoadBundleAsyncWrapper();
diff --git a/dist/plugin/babel-transformer.js b/dist/plugin/babel-transformer.js
index 5e6c8c0aa490aab4ce0469be04114454d9a1ae47..75c1b46c03cf082084c268e73000fc17b7dcd8ce 100644
--- a/dist/plugin/babel-transformer.js
+++ b/dist/plugin/babel-transformer.js
@@ -55,7 +55,8 @@ function createBabelTransformer({ blacklistedPaths, federationConfig, originalBa
         enableInitializeCorePatching ? '@module-federation/metro/babel-plugin/patch-initialize-core' : void 0,
         enableRuntimeRequirePatching ? '@module-federation/metro/babel-plugin/patch-require' : void 0
     ].filter(Boolean);
-    const babelTransformer = transformerTemplate.replaceAll('__BABEL_TRANSFORMER_PATH__', originalBabelTransformerPath).replaceAll('__BABEL_PLUGINS__', JSON.stringify(plugins));
+    const formattedBabelTransformerPath = process.platform === 'win32' ? originalBabelTransformerPath.replace(/\\/g, '\\\\') : originalBabelTransformerPath;
+    const babelTransformer = transformerTemplate.replaceAll('__BABEL_TRANSFORMER_PATH__', formattedBabelTransformerPath).replaceAll('__BABEL_PLUGINS__', JSON.stringify(plugins));
     external_node_fs_default().writeFileSync(outputPath, babelTransformer, 'utf-8');
     return outputPath;
 }
diff --git a/dist/plugin/babel-transformer.mjs b/dist/plugin/babel-transformer.mjs
index 7a1fb9b76937f5071edb60b7deb65c97a0357208..6531969720ff4f4b5cc40b8ceedd630281c17b4a 100644
--- a/dist/plugin/babel-transformer.mjs
+++ b/dist/plugin/babel-transformer.mjs
@@ -18,7 +18,8 @@ function createBabelTransformer({ blacklistedPaths, federationConfig, originalBa
         enableInitializeCorePatching ? '@module-federation/metro/babel-plugin/patch-initialize-core' : void 0,
         enableRuntimeRequirePatching ? '@module-federation/metro/babel-plugin/patch-require' : void 0
     ].filter(Boolean);
-    const babelTransformer = transformerTemplate.replaceAll('__BABEL_TRANSFORMER_PATH__', originalBabelTransformerPath).replaceAll('__BABEL_PLUGINS__', JSON.stringify(plugins));
+    const formattedBabelTransformerPath = process.platform === 'win32' ? originalBabelTransformerPath.replace(/\\/g, '\\\\') : originalBabelTransformerPath;
+    const babelTransformer = transformerTemplate.replaceAll('__BABEL_TRANSFORMER_PATH__', formattedBabelTransformerPath).replaceAll('__BABEL_PLUGINS__', JSON.stringify(plugins));
     node_fs.writeFileSync(outputPath, babelTransformer, 'utf-8');
     return outputPath;
 }
diff --git a/dist/plugin/constants.d.ts b/dist/plugin/constants.d.ts
index 006dce4b6e15a08fdd5e25dedd2c65b8b0ab56b8..2647144fd237effa7538be885086d4047c5ba370 100644
--- a/dist/plugin/constants.d.ts
+++ b/dist/plugin/constants.d.ts
@@ -7,3 +7,4 @@ export declare const GET_DEV_SERVER_REGEX: RegExp;
 export declare const HMR_CLIENT_REGEX: RegExp;
 export declare const TMP_DIR_NAME = ".mf-metro";
 export declare const MANIFEST_FILENAME = "mf-manifest.json";
+export declare const EXPO_VIRTUAL_METRO_ENTRY = ".expo/.virtual-metro-entry";
\ No newline at end of file
diff --git a/dist/plugin/constants.js b/dist/plugin/constants.js
index 2ae676f4a26a7a16c085777e6c23a03f5fe7852a..500d9d53165e6c5ced478e2cff779832abe73bd1 100644
--- a/dist/plugin/constants.js
+++ b/dist/plugin/constants.js
@@ -30,6 +30,7 @@ __webpack_require__.d(__webpack_exports__, {
     HMR_CLIENT_REGEX: ()=>HMR_CLIENT_REGEX,
     INIT_HOST: ()=>INIT_HOST,
     MANIFEST_FILENAME: ()=>MANIFEST_FILENAME,
+    EXPO_VIRTUAL_METRO_ENTRY: ()=>EXPO_VIRTUAL_METRO_ENTRY,
     REMOTE_HMR_SETUP: ()=>REMOTE_HMR_SETUP,
     REMOTE_MODULE_REGISTRY: ()=>REMOTE_MODULE_REGISTRY,
     TMP_DIR_NAME: ()=>TMP_DIR_NAME
@@ -43,12 +44,14 @@ const GET_DEV_SERVER_REGEX = /react-native\/Libraries\/Core\/Devtools\/getDevSer
 const HMR_CLIENT_REGEX = /react-native\/Libraries\/Utilities\/HMRClient\.js$/;
 const TMP_DIR_NAME = '.mf-metro';
 const MANIFEST_FILENAME = 'mf-manifest.json';
+const EXPO_VIRTUAL_METRO_ENTRY = '.expo/.virtual-metro-entry';
 exports.ASYNC_REQUIRE = __webpack_exports__.ASYNC_REQUIRE;
 exports.DEFAULT_ENTRY_FILENAME = __webpack_exports__.DEFAULT_ENTRY_FILENAME;
 exports.GET_DEV_SERVER_REGEX = __webpack_exports__.GET_DEV_SERVER_REGEX;
 exports.HMR_CLIENT_REGEX = __webpack_exports__.HMR_CLIENT_REGEX;
 exports.INIT_HOST = __webpack_exports__.INIT_HOST;
 exports.MANIFEST_FILENAME = __webpack_exports__.MANIFEST_FILENAME;
+exports.EXPO_VIRTUAL_METRO_ENTRY = __webpack_exports__.EXPO_VIRTUAL_METRO_ENTRY;
 exports.REMOTE_HMR_SETUP = __webpack_exports__.REMOTE_HMR_SETUP;
 exports.REMOTE_MODULE_REGISTRY = __webpack_exports__.REMOTE_MODULE_REGISTRY;
 exports.TMP_DIR_NAME = __webpack_exports__.TMP_DIR_NAME;
@@ -59,6 +62,7 @@ for(var __webpack_i__ in __webpack_exports__)if (-1 === [
     "HMR_CLIENT_REGEX",
     "INIT_HOST",
     "MANIFEST_FILENAME",
+    "EXPO_VIRTUAL_METRO_ENTRY",
     "REMOTE_HMR_SETUP",
     "REMOTE_MODULE_REGISTRY",
     "TMP_DIR_NAME"
diff --git a/dist/plugin/constants.mjs b/dist/plugin/constants.mjs
index f4db535b00b66db6df4f8c86cc1710c9a1f84229..3a8176d14891301c7e9ac29d36bac36ba9e468cb 100644
--- a/dist/plugin/constants.mjs
+++ b/dist/plugin/constants.mjs
@@ -9,4 +9,6 @@ const GET_DEV_SERVER_REGEX = /react-native\/Libraries\/Core\/Devtools\/getDevSer
 const HMR_CLIENT_REGEX = /react-native\/Libraries\/Utilities\/HMRClient\.js$/;
 const TMP_DIR_NAME = '.mf-metro';
 const MANIFEST_FILENAME = 'mf-manifest.json';
+const EXPO_VIRTUAL_METRO_ENTRY = '.expo/.virtual-metro-entry';
+export { ASYNC_REQUIRE, DEFAULT_ENTRY_FILENAME, GET_DEV_SERVER_REGEX, HMR_CLIENT_REGEX, INIT_HOST, MANIFEST_FILENAME, REMOTE_HMR_SETUP, REMOTE_MODULE_REGISTRY, TMP_DIR_NAME, EXPO_VIRTUAL_METRO_ENTRY };
 export { ASYNC_REQUIRE, DEFAULT_ENTRY_FILENAME, GET_DEV_SERVER_REGEX, HMR_CLIENT_REGEX, INIT_HOST, MANIFEST_FILENAME, REMOTE_HMR_SETUP, REMOTE_MODULE_REGISTRY, TMP_DIR_NAME };
diff --git a/dist/plugin/index.js b/dist/plugin/index.js
index bba9e215771021eb3fffd5960ab7b6f4f79cb39a..3bec6ee54cc058a507d9a7622b14609e48f07045 100644
--- a/dist/plugin/index.js
+++ b/dist/plugin/index.js
@@ -125,10 +125,11 @@ function augmentConfig(config, federationOptions, extraOptions) {
                     originalEntry: originalEntryPath,
                     hostEntry: hostEntryPath,
                     initHost: initHostPath,
+                    projectDir: config.projectRoot,
                     remoteModuleRegistry: remoteModuleRegistryPath,
                     remoteHMRSetup: remoteHMRSetupPath,
                     remoteEntry: remoteEntryPath,
-                    projectDir: config.projectRoot,
+                    serverRoot: config.server.unstable_serverRoot ?? config.projectRoot,
                     tmpDir: tmpDirPath
                 },
                 hacks: {
diff --git a/dist/plugin/index.mjs b/dist/plugin/index.mjs
index a986cec9b4599ec04442c8d88a5941299f4f8ccb..f1237a63653dda5c45ec4773b32d277db52a4c94 100644
--- a/dist/plugin/index.mjs
+++ b/dist/plugin/index.mjs
@@ -88,10 +88,11 @@ function augmentConfig(config, federationOptions, extraOptions) {
                     originalEntry: originalEntryPath,
                     hostEntry: hostEntryPath,
                     initHost: initHostPath,
+                    projectDir: config.projectRoot,
                     remoteModuleRegistry: remoteModuleRegistryPath,
                     remoteHMRSetup: remoteHMRSetupPath,
                     remoteEntry: remoteEntryPath,
-                    projectDir: config.projectRoot,
+                    serverRoot: config.server.unstable_serverRoot ?? config.projectRoot,
                     tmpDir: tmpDirPath
                 },
                 hacks: {
diff --git a/dist/plugin/resolver.d.ts b/dist/plugin/resolver.d.ts
index 6fe4cd54ef820e7f748d210f73cb72abd02f6432..7efaea64346c1641acb7f5f529216b4125028944 100644
--- a/dist/plugin/resolver.d.ts
+++ b/dist/plugin/resolver.d.ts
@@ -12,10 +12,11 @@ interface CreateResolveRequestOptions {
         hostEntry: string;
         initHost: string;
         originalEntry: string;
+        projectDir: string;
         remoteEntry: string;
         remoteHMRSetup: string;
         remoteModuleRegistry: string;
-        projectDir: string;
+        serverRoot: string;
         tmpDir: string;
     };
     options: ModuleFederationConfigNormalized;
diff --git a/dist/plugin/resolver.js b/dist/plugin/resolver.js
index 2ba26e1650598de48390bc3ea9e459284d622731..bf3815117480e5dfc2c011679bdd05b5503edc2e 100644
--- a/dist/plugin/resolver.js
+++ b/dist/plugin/resolver.js
@@ -43,11 +43,11 @@ const external_helpers_js_namespaceObject = require("./helpers.js");
 function createResolveRequest({ vmManager, options, hacks, paths, isRemote, customResolver }) {
     const hostEntryPathRegex = getEntryPathRegex({
         entry: paths.hostEntry,
-        projectDir: paths.projectDir
+        serverRoot: paths.serverRoot
     });
     const remoteEntryPathRegex = getEntryPathRegex({
         entry: paths.remoteEntry,
-        projectDir: paths.projectDir
+        serverRoot: paths.serverRoot
     });
     return function(context, moduleName, platform) {
         if (moduleName.match(hostEntryPathRegex)) {
@@ -186,9 +186,9 @@ function replaceModule(from, to) {
     };
 }
 function getEntryPathRegex(paths) {
-    const relativeEntryPath = external_node_path_default().relative(paths.projectDir, paths.entry);
+    const relativeEntryPath = external_node_path_default().relative(paths.serverRoot, paths.entry);
     const entryName = (0, external_helpers_js_namespaceObject.removeExtension)(relativeEntryPath);
-    return new RegExp(`^\\./${entryName}(\\.js)?$`);
+    return new RegExp(`/${entryName}(\\.js)?$`);
 }
 exports.createResolveRequest = __webpack_exports__.createResolveRequest;
 for(var __webpack_i__ in __webpack_exports__)if (-1 === [
diff --git a/dist/plugin/resolver.mjs b/dist/plugin/resolver.mjs
index acd8a97f1c7ddb478adc1028f22ebed6b3475652..22d85c0ce69954009217e1c716d19f89db996e03 100644
--- a/dist/plugin/resolver.mjs
+++ b/dist/plugin/resolver.mjs
@@ -10,11 +10,11 @@ var resolver_dirname = __webpack_dirname__(__webpack_fileURLToPath__(import.meta
 function createResolveRequest({ vmManager, options, hacks, paths, isRemote, customResolver }) {
     const hostEntryPathRegex = getEntryPathRegex({
         entry: paths.hostEntry,
-        projectDir: paths.projectDir
+        serverRoot: paths.serverRoot
     });
     const remoteEntryPathRegex = getEntryPathRegex({
         entry: paths.remoteEntry,
-        projectDir: paths.projectDir
+        serverRoot: paths.serverRoot
     });
     return function(context, moduleName, platform) {
         if (moduleName.match(hostEntryPathRegex)) {
@@ -153,8 +153,8 @@ function replaceModule(from, to) {
     };
 }
 function getEntryPathRegex(paths) {
-    const relativeEntryPath = node_path.relative(paths.projectDir, paths.entry);
+    const relativeEntryPath = node_path.relative(paths.serverRoot, paths.entry);
     const entryName = removeExtension(relativeEntryPath);
-    return new RegExp(`^\\./${entryName}(\\.js)?$`);
+    return new RegExp(`/${entryName}(\\.js)?$`);
 }
 export { createResolveRequest };
diff --git a/dist/plugin/rewrite-request.js b/dist/plugin/rewrite-request.js
index 90731157546daf235cb278607857869c52a6b2db..d70ae65f777816016875428326e7619674b66e06 100644
--- a/dist/plugin/rewrite-request.js
+++ b/dist/plugin/rewrite-request.js
@@ -40,11 +40,15 @@ var external_node_path_default = /*#__PURE__*/ __webpack_require__.n(external_no
 const external_constants_js_namespaceObject = require("./constants.js");
 const external_helpers_js_namespaceObject = require("./helpers.js");
 function createRewriteRequest({ config, originalEntryFilename, remoteEntryFilename, manifestPath, tmpDirPath }) {
+    const root = config.server.unstable_serverRoot ?? config.projectRoot;
     const hostEntryName = (0, external_helpers_js_namespaceObject.removeExtension)(originalEntryFilename);
     const remoteEntryName = (0, external_helpers_js_namespaceObject.removeExtension)(remoteEntryFilename);
-    const relativeTmpDirPath = external_node_path_default().relative(config.projectRoot, tmpDirPath).split(external_node_path_default().sep).join(external_node_path_default().posix.sep);
+    const relativeTmpDirPath = external_node_path_default().relative(root, tmpDirPath).split(external_node_path_default().sep).join(external_node_path_default().posix.sep);
     const hostEntryPathRegex = getEntryPathRegex(hostEntryName);
     const remoteEntryPathRegex = getEntryPathRegex(remoteEntryName);
+    const virtualMetroEntryPathRegex = getEntryPathRegex(
+        external_constants_js_namespaceObject.EXPO_VIRTUAL_METRO_ENTRY,
+    );
     return function(url) {
         const root = config.projectRoot;
         const { pathname } = new URL(url, 'protocol://host');
@@ -56,8 +60,15 @@ function createRewriteRequest({ config, originalEntryFilename, remoteEntryFilena
             const target = `${relativeTmpDirPath}/${remoteEntryName}`;
             return url.replace(remoteEntryName, target);
         }
+        if (pathname.match(virtualMetroEntryPathRegex)) {
+            const target = `${relativeTmpDirPath}/${hostEntryName}`;
+            return url.replace(external_constants_js_namespaceObject.EXPO_VIRTUAL_METRO_ENTRY, target);
+        }
         if (pathname.startsWith(`/${external_constants_js_namespaceObject.MANIFEST_FILENAME}`)) {
-            const target = manifestPath.replace(root, '[metro-project]');
+            const target = manifestPath.replace(
+                config.projectRoot,
+                '[metro-project]',
+            );
             return url.replace(external_constants_js_namespaceObject.MANIFEST_FILENAME, target);
         }
         if (config.server.rewriteRequestUrl) return config.server.rewriteRequestUrl(url);
@@ -65,7 +76,7 @@ function createRewriteRequest({ config, originalEntryFilename, remoteEntryFilena
     };
 }
 function getEntryPathRegex(entryFilename) {
-    return new RegExp(`^\\/${entryFilename}(\\.js)?(\\.bundle)$`);
+    return new RegExp(`\\/${entryFilename}(\\.js)?(\\.bundle)$`);
 }
 exports.createRewriteRequest = __webpack_exports__.createRewriteRequest;
 for(var __webpack_i__ in __webpack_exports__)if (-1 === [
diff --git a/dist/plugin/rewrite-request.mjs b/dist/plugin/rewrite-request.mjs
index 4a43538c0b8625df86f42e01832e607222cd899d..456af738bbb5b1b2e7cebe10c30e60cffaac0568 100644
--- a/dist/plugin/rewrite-request.mjs
+++ b/dist/plugin/rewrite-request.mjs
@@ -1,14 +1,16 @@
 import 'module';
 /*#__PURE__*/ import.meta.url;
 import node_path from "node:path";
-import { MANIFEST_FILENAME } from "./constants.mjs";
+import { EXPO_VIRTUAL_METRO_ENTRY, MANIFEST_FILENAME } from "./constants.mjs";
 import { removeExtension } from "./helpers.mjs";
 function createRewriteRequest({ config, originalEntryFilename, remoteEntryFilename, manifestPath, tmpDirPath }) {
+    const root = config.server.unstable_serverRoot ?? config.projectRoot;
     const hostEntryName = removeExtension(originalEntryFilename);
     const remoteEntryName = removeExtension(remoteEntryFilename);
-    const relativeTmpDirPath = node_path.relative(config.projectRoot, tmpDirPath).split(node_path.sep).join(node_path.posix.sep);
+    const relativeTmpDirPath = node_path.relative(root, tmpDirPath).split(node_path.sep).join(node_path.posix.sep);
     const hostEntryPathRegex = getEntryPathRegex(hostEntryName);
     const remoteEntryPathRegex = getEntryPathRegex(remoteEntryName);
+    const virtualMetroEntryPathRegex = getEntryPathRegex(EXPO_VIRTUAL_METRO_ENTRY);
     return function(url) {
         const root = config.projectRoot;
         const { pathname } = new URL(url, 'protocol://host');
@@ -16,12 +18,19 @@ function createRewriteRequest({ config, originalEntryFilename, remoteEntryFilena
             const target = `${relativeTmpDirPath}/${hostEntryName}`;
             return url.replace(hostEntryName, target);
         }
+        if (pathname.match(virtualMetroEntryPathRegex)) {
+            const target = `${relativeTmpDirPath}/${hostEntryName}`;
+            return url.replace(EXPO_VIRTUAL_METRO_ENTRY, target);
+        }
         if (pathname.match(remoteEntryPathRegex)) {
             const target = `${relativeTmpDirPath}/${remoteEntryName}`;
             return url.replace(remoteEntryName, target);
         }
         if (pathname.startsWith(`/${MANIFEST_FILENAME}`)) {
-            const target = manifestPath.replace(root, '[metro-project]');
+            const target = manifestPath.replace(
+                config.projectRoot,
+                '[metro-project]',
+            );
             return url.replace(MANIFEST_FILENAME, target);
         }
         if (config.server.rewriteRequestUrl) return config.server.rewriteRequestUrl(url);
@@ -29,6 +38,6 @@ function createRewriteRequest({ config, originalEntryFilename, remoteEntryFilena
     };
 }
 function getEntryPathRegex(entryFilename) {
-    return new RegExp(`^\\/${entryFilename}(\\.js)?(\\.bundle)$`);
+    return new RegExp(`\\/${entryFilename}(\\.js)?(\\.bundle)$`);
 }
 export { createRewriteRequest };
diff --git a/dist/plugin/serializer.js b/dist/plugin/serializer.js
index a3b8a6cff7d68dba44fb76d69261835ad106a454..357961f6db0e2b4472791c7d47499ab06f73a259 100644
--- a/dist/plugin/serializer.js
+++ b/dist/plugin/serializer.js
@@ -57,7 +57,7 @@ function getModuleFederationSerializer(mfConfig, isUsingMFBundleCommand) {
             return getBundleCode(entryPoint, finalPreModules, graph, options);
         }
         if (!isProjectSource(entryPoint, options.projectRoot)) return getBundleCode(entryPoint, preModules, graph, options);
-        const bundlePath = getBundlePath(entryPoint, options.projectRoot, mfConfig.exposes, isUsingMFBundleCommand);
+        const bundlePath = getBundlePath(entryPoint, options.serverRoot, mfConfig.exposes, isUsingMFBundleCommand);
         const finalPreModules = [
             getSyncShared(syncSharedModules, bundlePath, mfConfig.name),
             getSyncRemotes(syncRemoteModules, bundlePath, mfConfig.name)
@@ -140,8 +140,8 @@ function isProjectSource(entryPoint, projectRoot) {
     const relativePath = external_node_path_default().relative(projectRoot, entryPoint);
     return !relativePath.startsWith('..') && !relativePath.startsWith('node_modules');
 }
-function getBundlePath(entryPoint, projectRoot, exposes, isUsingMFBundleCommand) {
-    const relativeEntryPath = external_node_path_default().relative(projectRoot, entryPoint);
+function getBundlePath(entryPoint, serverRoot, exposes, isUsingMFBundleCommand) {
+    const relativeEntryPath = external_node_path_default().relative(serverRoot, entryPoint);
     if (!isUsingMFBundleCommand) {
         const { dir, name } = external_node_path_default().parse(relativeEntryPath);
         return external_node_path_default().format({
@@ -156,7 +156,7 @@ function getBundlePath(entryPoint, projectRoot, exposes, isUsingMFBundleCommand)
         if (exposedName.startsWith('./')) exposedName = exposedName.slice(2);
         return `exposed/${exposedName}`;
     }
-    throw new errors_js_namespaceObject.ConfigError(`Unable to handle entry point: ${relativeEntryPath}. Expected to match an entrypoint with one of the exposed keys, but failed. This is most likely a configuration error. If you believe this is not a configuration issue, please report it as a bug. Debug info: entryPoint="${entryPoint}", projectRoot="${projectRoot}", exposesKeys=[${Object.keys(exposes).join(', ')}]`);
+    throw new errors_js_namespaceObject.ConfigError(`Unable to handle entry point: ${relativeEntryPath}. Expected to match an entrypoint with one of the exposed keys, but failed. This is most likely a configuration error. If you believe this is not a configuration issue, please report it as a bug. Debug info: entryPoint="${entryPoint}", serverRoot="${serverRoot}", exposesKeys=[${Object.keys(exposes).join(', ')}]`);
 }
 function getBundleCode(entryPoint, preModules, graph, options) {
     const { code } = bundleToString_default()(baseJSBundle_default()(entryPoint, preModules, graph, options));
diff --git a/dist/plugin/serializer.mjs b/dist/plugin/serializer.mjs
index 8b706d9cfb1c779720bb317a4b00f1d28fcc4887..6904b061189166eac71b1e3e637a5faf8858e2da 100644
--- a/dist/plugin/serializer.mjs
+++ b/dist/plugin/serializer.mjs
@@ -43,7 +43,7 @@ function getModuleFederationSerializer(mfConfig, isUsingMFBundleCommand) {
             return getBundleCode(entryPoint, finalPreModules, graph, options);
         }
         if (!isProjectSource(entryPoint, options.projectRoot)) return getBundleCode(entryPoint, preModules, graph, options);
-        const bundlePath = getBundlePath(entryPoint, options.projectRoot, mfConfig.exposes, isUsingMFBundleCommand);
+        const bundlePath = getBundlePath(entryPoint, options.serverRoot, mfConfig.exposes, isUsingMFBundleCommand);
         const finalPreModules = [
             getSyncShared(syncSharedModules, bundlePath, mfConfig.name),
             getSyncRemotes(syncRemoteModules, bundlePath, mfConfig.name)
@@ -126,8 +126,8 @@ function isProjectSource(entryPoint, projectRoot) {
     const relativePath = node_path.relative(projectRoot, entryPoint);
     return !relativePath.startsWith('..') && !relativePath.startsWith('node_modules');
 }
-function getBundlePath(entryPoint, projectRoot, exposes, isUsingMFBundleCommand) {
-    const relativeEntryPath = node_path.relative(projectRoot, entryPoint);
+function getBundlePath(entryPoint, serverRoot, exposes, isUsingMFBundleCommand) {
+    const relativeEntryPath = node_path.relative(serverRoot, entryPoint);
     if (!isUsingMFBundleCommand) {
         const { dir, name } = node_path.parse(relativeEntryPath);
         return node_path.format({
@@ -142,7 +142,7 @@ function getBundlePath(entryPoint, projectRoot, exposes, isUsingMFBundleCommand)
         if (exposedName.startsWith('./')) exposedName = exposedName.slice(2);
         return `exposed/${exposedName}`;
     }
-    throw new ConfigError(`Unable to handle entry point: ${relativeEntryPath}. Expected to match an entrypoint with one of the exposed keys, but failed. This is most likely a configuration error. If you believe this is not a configuration issue, please report it as a bug. Debug info: entryPoint="${entryPoint}", projectRoot="${projectRoot}", exposesKeys=[${Object.keys(exposes).join(', ')}]`);
+    throw new ConfigError(`Unable to handle entry point: ${relativeEntryPath}. Expected to match an entrypoint with one of the exposed keys, but failed. This is most likely a configuration error. If you believe this is not a configuration issue, please report it as a bug. Debug info: entryPoint="${entryPoint}", serverRoot="${serverRoot}", exposesKeys=[${Object.keys(exposes).join(', ')}]`);
 }
 function getBundleCode(entryPoint, preModules, graph, options) {
     const { code } = bundleToString_default()(baseJSBundle_default()(entryPoint, preModules, graph, options));
diff --git a/dist/runtime/remote-entry.js b/dist/runtime/remote-entry.js
index d71bf2971a2800b5f3c470e45c3f04a55b1ab51f..e8d93e880f052a1b6c7c504166666f33bf124c63 100644
--- a/dist/runtime/remote-entry.js
+++ b/dist/runtime/remote-entry.js
@@ -1,4 +1,3 @@
-import 'mf:async-require';
 import { loadSharedToRegistry } from 'mf:remote-module-registry';
 import { init as runtimeInit } from '@module-federation/runtime';
 __PLUGINS__;
@@ -35,6 +34,9 @@ async function init(shared = {}, initScope = []) {
         initScope
     }));
     __EARLY_SHARED__.forEach(loadSharedToRegistry);
+
+    require('mf:async-require');
+    
     await initSharingPromise;
     if (__DEV__ && !hmrInitialized) {
         const hmr = require('mf:remote-hmr');
